#pragma once

// Class name: Time
// public part
     // default constructor that initializes hour = 0 and minutes = 0 
     // constructor with parameters that receives hour and minutes (in that order)
     // getters and setters for each one of the attributes
     // method elapsedTime that receives as a parameter an object of type Time and returns an integer value
     // it calculates the elapsedTime of an evento that starts at the time of the current object
     // and finishes at the time of the object received as parameter.
     // Tip: you can convert both times to minutes to calculate the difference
// private part
    // integer attibutes hour and minutes


